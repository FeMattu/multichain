# Python packages NOT taken from the Ubuntu 22.04 archive.
#
# Everything else in the analysis stack comes from apt on purpose: on 22.04 the
# apt versions are the ones that produced the archived campaign (networkx 2.4,
# numpy 1.21.5, pandas 1.3.5, scipy 1.8.0, jsonschema 3.2.0), and networkx in
# particular is load-bearing - its GML reader/writer changed behaviour after
# 2.4, and the harness still reads and writes GML for the analysis pipeline's
# edge tables.
#
# openpyxl: apt ships 3.0.9, the archived .xlsx workbooks were written with
# 3.1.5.
openpyxl==3.1.5

# The build context is this directory only: the repository itself is
# bind-mounted at runtime, never copied into the image.
.env
README.md
docker-compose.yml
host-tune.sh
mcsim

# Host-install reference for CORE, not part of the image: the Dockerfile
# installs CORE itself.
CORE_sim/

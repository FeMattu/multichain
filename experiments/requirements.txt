# Runtime and analysis dependencies of the emulation harness.
#
# Pinned as lower bounds, not exact versions: the target machine may be Ubuntu
# 20.04, whose distribution packages are older than PyPI's current releases,
# and every bound below is satisfied by the 20.04 archive. `scripts/
# check_environment.sh` reports what is present and what is only optional.

# --- mandatory --------------------------------------------------------------
PyYAML>=5.3            # configuration files
jsonschema>=3.2         # schema validation, offline (no remote $ref)

# --- analysis (mandatory for `analysis run` and `report generate`) ----------
numpy>=1.17
scipy>=1.3              # chi-square, Spearman p-values, KS
pandas>=1.0
matplotlib>=3.1         # plots
networkx>=2.4           # GML round-trip and the historical latency matrix
openpyxl>=3.0           # the per-run .xlsx workbook the campaign produces

# --- optional ---------------------------------------------------------------
# CORE Network Emulator's Python API. Not on PyPI: it is installed with CORE
# itself (see docs/troubleshooting.md). Absent, the harness falls back to the
# netns fabric and says so.
# core-python
